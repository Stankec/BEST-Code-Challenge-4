<table class="table table-striped">
	<tbody>	
		<% Movie.all.each do |m| %>
			<tr>
				<td><%= m.id %></td>
				<td><%= m.title %></td>
				<td><%= m.year %></td>
				<td><%= link_to "Delete", movie_path(m), :method => "delete" %></td>
			</tr>
		<% end %>
	</tbody>
</table>
